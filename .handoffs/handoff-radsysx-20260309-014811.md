# Workflow Handoff: radsysx

> **Session**: 2026-03-09T01:48:11Z
> **Agent**: GPT-5.4
> **Handoff Reason**: checkpoint

---

## Ξ State Vector

**Objective**: Stabilize the governed clinical OHIF viewer flow under the one-origin compose stack on native Ubuntu.

**Phase**: Post-fix cleanup and resume prep | Progress: 95% | Status: paused

**Current Focus**:
The governed clinical compose flow now works through `login -> worklist -> /viewer` with the OHIF viewport rendered and the RadSysX workspace panel mounted inside the right sidebar. The debug instrumentation that was added during investigation has been removed from source/build files, and the `viewer` service was restarted so the served assets were regenerated from the cleaned sources. Work is on branch `new-dev-1-31-26`; the working tree is effectively clean except for untracked `.serena/`.

**Blocker**:
- Host-side `npm run build --workspace viewer` can still fail if `viewer/dist` is root-owned after container rebuilds.
- Resolution path: remove or `chown` `viewer/dist` on the host before running the host-side viewer build, or continue to rely on the container-side `viewer` rebuild path.

---

## Δ Context Frame

### Decisions Log
| ID | Decision | Rationale | Reversible |
|----|----------|-----------|------------|
| D1 | Set the clinical OHIF mode `routeName` to `""` | `routerBasename` is already `/viewer`; using `viewer` prevented `ModeRoute` from rendering under the basename root | yes |
| D2 | Remove the `@radsysx/extension-clinical` mode `extensions` entry | The RadSysX OHIF extension is injected globally via `window.__RADSYSX_OHIF_EXTENSION__`; the explicit module specifier caused runtime resolution failure | yes |
| D3 | Normalize viewer DICOMweb roots to absolute same-origin URLs | Root-relative `/dicom-web` values were being consumed as path-relative requests in live OHIF runtime | yes |
| D4 | Explicitly load Orthanc's DICOMweb plugin in `orthanc.json` | The shared library existed in the image, but the running Orthanc instance had not loaded the plugin and `/dicom-web/*` returned `Unknown resource` | yes |
| D5 | Use `multipart/related` for STOW uploads | Orthanc's DICOMweb endpoint rejected single-file `application/dicom` uploads with `415 Unsupported Media Type` | yes |
| D6 | Force-activate the RadSysX workspace panel in `onModeEnter` | The panel was registered in the right panel list but remained inactive, so the custom element never mounted and the loader never cleared | yes |

### Constraints Active
- Governed clinical flow must remain backend-authoritative: session cookie identity, opaque launch token, backend-mediated STOW.
- OHIF is the only supported clinical viewer under `/viewer`; no Next.js legacy fallback is allowed.
- Do not put PHI-bearing launch context into URLs.
- Research and clinical surfaces remain distinct; do not route clinical fixes through research-only seams.

### Patterns In Use
- **Minimal bootstrap**: resolve session + launch, apply runtime DICOMweb roots, strip transient URL state → See `viewer/assets/radsysx-bootstrap.js:25-196`
- **OHIF-native workspace integration**: RadSysX UI is a panel module activated from the OHIF mode layer → See `viewer/assets/radsysx-ohif-extension.js:17-85`, `viewer/assets/radsysx-ohif-mode.js:13-45`
- **Backend-mediated multipart STOW**: both derived-result uploads and Orthanc seed uploads use `multipart/related` → See `backend/clinical/dicomweb.py:165-206`, `backend/clinical/seed_orthanc.py:94-142`

### Mental Models Required
Basename-root routing
: With `routerBasename="/viewer"`, the clinical mode route must be the basename root (`routeName=""`), not `viewer`.

Injected OHIF extension
: `window.__RADSYSX_OHIF_EXTENSION__` is the active extension object; do not also declare it as an importable module specifier in mode config.

Orthanc DICOMweb readiness
: `DicomWeb.Enable` in config is not sufficient by itself; the plugin must be loaded and `/plugins` must include `"dicom-web"` before `/dicom-web/*` works.

---

## Φ Code Map

### Modified This Session
| File | Lines | Change Summary |
|------|-------|----------------|
| `viewer/assets/radsysx-ohif-mode.js` | 3-49 | Align clinical mode with `/viewer` basename, append workspace panel, and force-activate the workspace tab in `onModeEnter` |
| `viewer/assets/radsysx-ohif-extension.js` | 17-85, 379-562 | Mount the RadSysX workspace panel, remove the loader after workspace fetch, wrap the OHIF dicomweb data source, inject launch-backed study UID, and normalize DICOMweb runtime roots |
| `viewer/assets/radsysx-bootstrap.js` | 25-196 | Resolve governed launch, strip transient URL params, and normalize DICOMweb roots onto the app config before OHIF starts |
| `viewer/scripts/build-ohif-dist.mjs` | 31-38, 111-195, 251-264 | Copy the RadSysX viewer assets into copied OHIF `dist`, inject a dynamic `<base href>`, patch worker/public-path behavior, and normalize service-worker precache URLs |
| `deploy/clinical-stack/orthanc.json` | 2-15 | Explicitly load `libOrthancDicomWeb.so` and keep the DICOMweb root at `/dicom-web/` |
| `backend/clinical/dicomweb.py` | 165-206 | Switch uploaded-instance STOW requests to `multipart/related` |
| `backend/clinical/seed_orthanc.py` | 94-142 | Seed sample studies through DICOMweb using `multipart/related` |

### Reference Anchors
| File | Lines | Relevance |
|------|-------|-----------|
| `AGENTS.md` | 89-97 | Canonical clinical workflow sequence: login, worklist, launch, `/viewer/?launch=...`, resolve, workspace |
| `AGENTS.md` | 197-208 | Viewer-direction guidance: OHIF-only clinical viewer under `viewer/` |
| `PHASE4_CLINICAL_EXECUTION_CHECKLIST.md` | 63-70 | End-to-end compose validation goals for this tranche |
| `README.md` | 47-58 | Public clinical workflow description that should stay aligned with shipped behavior |
| `docker-compose.yml` | 37-87 | Backend, Orthanc, and `orthanc-seed` service definitions used in local one-origin validation |
| `backend/tests/test_clinical_platform.py` | 174-219 | Backend launch/runtime contract tests for opaque viewer URLs and same-origin DICOMweb roots |
| `backend/tests/test_clinical_platform.py` | 302-347 | Backend STOW test coverage for derived-result registration |

### Entry Points
- **Primary**: `viewer/assets/radsysx-bootstrap.js:25-81` — governed launch/session bootstrap and runtime application
- **Primary**: `viewer/assets/radsysx-ohif-mode.js:13-45` — clinical OHIF mode routing and workspace-panel activation
- **Test Suite**: `backend/tests/test_clinical_platform.py` — covers launch contracts, runtime roots, and backend STOW behavior

---

## Ψ Knowledge Prerequisites

### Documentation Sections
- [ ] `AGENTS.md` § `Clinical path` and `Viewer direction` — authoritative architecture and runtime rules
- [ ] `PHASE4_CLINICAL_EXECUTION_CHECKLIST.md` § `Validate the local one-origin clinical stack end to end` — current tranche goals
- [ ] `README.md` § `Clinical Workflow` — public-facing architecture summary to keep aligned

### Modules to Explore
- [ ] `viewer/assets/radsysx-ohif-mode.js` — understand the basename-root routing and forced workspace-panel activation
- [ ] `viewer/assets/radsysx-ohif-extension.js` — understand the workspace panel, launch-backed data source wrapper, and DICOMweb root normalization
- [ ] `viewer/scripts/build-ohif-dist.mjs` — understand how copied OHIF `dist` is rewritten for `/viewer/`
- [ ] `backend/clinical/dicomweb.py` — understand multipart STOW behavior used by viewer writeback and seed flows
- [ ] `backend/clinical/seed_orthanc.py` — understand how local seeded studies are loaded into Orthanc

---

## Ω Forward Vector

### Next Actions *(priority order)*
1. **Align docs**: Update maintained docs to reflect what was actually verified in the clinical compose stack → `README.md`, `PHASE4_CLINICAL_EXECUTION_CHECKLIST.md`
2. **Package changes**: Review the current branch state and create a commit/PR if desired → viewer + Orthanc + backend files above
3. **Run one final no-debug smoke pass**: Reconfirm `login -> worklist -> viewer viewport -> RadSysX workspace panel` under `http://localhost:3000`
4. **Optional hardening**: Address host-side `viewer/dist` ownership so local host builds do not require manual cleanup

### Open Questions
- [ ] Should the `docker-compose.yml` viewer service be adjusted to avoid producing root-owned `viewer/dist` on host bind mounts?
- [ ] Should docs explicitly call out that Orthanc seed may need rerun if DICOMweb plugin configuration changes on an existing local volume?

### Success Criteria
- [ ] Maintained docs accurately describe the shipped OHIF clinical path and Orthanc/DICOMweb behavior
- [ ] The branch is packaged cleanly for commit/PR without debug instrumentation
- [ ] A fresh compose run still reaches OHIF viewport + RadSysX workspace panel under `http://localhost:3000/viewer/`

### Hazards / Watch Points
- ⚠️ Host-side `viewer/dist` ownership can still break local `npm run build --workspace viewer` after container-side rebuilds
- ⚠️ Do not regress the OHIF route back to `routeName: "viewer"` while `routerBasename` remains `/viewer`
- ⚠️ Do not remove explicit DICOMweb plugin loading from `deploy/clinical-stack/orthanc.json` unless plugin loading is validated another way

---

## Glossary *(session-specific terms)*
| Term | Definition |
|------|------------|
| Basename-root route | The OHIF clinical route mounted at the router basename root, implemented as `routeName=""` under `/viewer` |
| Workspace panel | The RadSysX right-side OHIF panel that renders report/AI/derived-result/audit UI |
| Multipart STOW | `multipart/related; type="application/dicom"` upload format required by the live Orthanc DICOMweb endpoint in this stack |
